Este archivo XML representa un panel de vuelos con información sobre el aeropuerto, fecha y detalles de los vuelos (origen, destino, horarios y estado). Está validado mediante un DTD externo (vuelos.dtd).

Para validar, coloca vuelos.dtd en la misma carpeta que el archivo XML y utiliza un validador XML para comprobar la estructura.